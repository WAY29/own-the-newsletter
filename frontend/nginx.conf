server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  access_log off;

  location /admin/ {
    rewrite ^/admin/(.*)$ /$1 break;
    try_files $uri $uri/ /index.html;
  }

  location = /admin {
    return 301 /admin/;
  }
}
