# Cloudflare Worker Runtime Out of First Version Scope

The first version does not design for Cloudflare Workers compatibility and instead targets a Docker Compose deployment with FastAPI, React, Nginx, SQLite, and local feed files. Static hosting through GitHub Pages or Cloudflare Pages is treated as external RSS publication, not as Cloudflare runtime support.
