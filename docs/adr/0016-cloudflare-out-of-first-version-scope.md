# Cloudflare Out of First Version Scope

The first version does not design for Cloudflare Workers compatibility and instead targets a Docker Compose deployment with FastAPI, React, Nginx, SQLite, and local feed files. Cloudflare support can be revisited later as a separate architecture effort rather than constraining the initial product model.
