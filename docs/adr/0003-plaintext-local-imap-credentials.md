# Plaintext Local IMAP Credentials

Status: superseded by ADR-0004

IMAP credentials were originally planned to be provided in local configuration, including plaintext passwords, because the product is a self-hosted personal tool intended to run on the user's own computer or server. This was superseded when the first version moved RSS/feed configuration into a web admin panel backed by local SQLite.
