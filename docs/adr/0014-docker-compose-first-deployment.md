# Docker Compose First Deployment

The first deployable version targets Docker Compose so the FastAPI backend, React admin frontend, and same-origin reverse proxy can be run together with explicit environment configuration. Local development can still run services separately, but Docker Compose is the primary packaging path for personal server deployment.
