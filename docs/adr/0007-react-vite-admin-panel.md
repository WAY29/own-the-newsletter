# React Vite Admin Panel

The admin panel uses React with Vite and shadcn/ui as a separate frontend application that calls the FastAPI backend. This avoids introducing a Next.js server or full-stack frontend runtime that would overlap with FastAPI, while still giving the project a modern TypeScript UI for managing IMAP accounts and feed rules.
